import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.lxuan.kekememo',
  appName: '課刻',
  webDir: 'dist',
  android: {
    backgroundColor: '#f3eee4',
  },
}

export default config
